Use AspNetAuth

GO

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

ALTER PROCEDURE SP_getDDLSaleList
AS
BEGIN

	SET NOCOUNT ON;

	SELECT YEAR(SaleDate) AS FiscalYear FROM Sales WITH(NOLOCK) GROUP BY YEAR(SaleDate) order by YEAR(SaleDate) DESC

END;
GO